═══════════════════════════════════════════════
  QUICK FIX - CONTENT SCRIPT NOT INJECTING
═══════════════════════════════════════════════

Your console shows: "Extension installed: null"
Meaning: Extension not running on page.

═══════════════════════════════════════════════
  FIX IN 60 SECONDS
═══════════════════════════════════════════════

1. Open new Chrome tab
2. Type in address bar: chrome://extensions/
3. Press Enter

4. Look for "SAFNEX NOVA Link Guard"
   
   IF YOU SEE IT:
   → Click the circular reload icon (🔄)
   → Wait 2 seconds
   → Close and reopen DEBUG-EXTENSION.html
   → Check console again
   
   IF YOU DON'T SEE IT:
   → Click "Load unpacked" button (top left)
   → Navigate to: C:\Users\MUTHU KRISHNAN\Downloads\ai-main\ai-main\extension
   → Click "Select Folder"
   → Extension will load
   → Open DEBUG-EXTENSION.html
   → Check console

5. After reload, refresh DEBUG-EXTENSION.html
6. Press F12
7. Console should now show: "Extension installed: true"

═══════════════════════════════════════════════
  IF STILL SHOWS NULL
═══════════════════════════════════════════════

Extension loaded but not working on localhost.

Try on real website instead:
1. Open new tab
2. Go to: https://www.google.com
3. F12 → Console
4. Type: document.documentElement.getAttribute('data-safnex-installed')
5. Should show "true"

If "true" on google.com but not on localhost:
→ This is NORMAL
→ Extensions often don't inject into file:/// URLs
→ Test on actual website instead

═══════════════════════════════════════════════
  TEST ON REAL SITE
═══════════════════════════════════════════════

1. Go to: https://example.com
2. F12 → Console
3. Check: document.documentElement.getAttribute('data-safnex-installed')
4. Should show: "true"
5. Click any external link
6. Popup should appear with 2 buttons

═══════════════════════════════════════════════

TL;DR:
1. chrome://extensions/
2. Find SAFNEX → Click reload (🔄)
3. Test on https://example.com (not localhost)
4. Click link → Popup appears

